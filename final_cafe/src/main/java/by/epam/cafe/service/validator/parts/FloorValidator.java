package by.epam.cafe.service.validator.parts;

public class FloorValidator implements by.epam.cafe.service.validator.Validator<Integer> {
    @Override
    public boolean isValid(Integer input) {
        return true;
    }
}
